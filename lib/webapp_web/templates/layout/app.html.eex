<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>Webapp · Phoenix Framework</title>
    <link rel="stylesheet" href="<%= Routes.static_path(@conn, "/css/app.css") %>"/>
  </head>
  <body>
    <nav class="navbar" role="navigation" aria-label="main navigation">
        <div class="container">
          <div class="navbar-brand">
            <a class="navbar-item" href="/">
               Runhyve
            </a>

            <a role="button" class="navbar-burger burger" aria-label="menu" aria-expanded="false" data-target="navbarBasicExample">
              <span aria-hidden="true"></span>
              <span aria-hidden="true"></span>
              <span aria-hidden="true"></span>
            </a>
          </div>

          <div id="navbarBasicExample" class="navbar-menu">
            <div class="navbar-start">
              <%= link "Home", to: Routes.page_path(@conn, :index), class: "navbar-item" %>

              <%= if @current_user do %>
                <%= link "My Teams", to: Routes.user_path(@conn, :teams, @current_user), class: "navbar-item" %>
              <% end %>

              <%= if false do %>
               <%= link "Users", to: Routes.user_path(@conn, :index), class: "navbar-item" %>
                <%= link "Teams", to: Routes.team_path(@conn, :index), class: "navbar-item" %>
                <%= link "Plans", to: Routes.plan_path(@conn, :index), class: "navbar-item" %>
                <%= link "Hypervisors", to: Routes.hypervisor_path(@conn, :index), class: "navbar-item" %>
                <%= link "Machines", to: Routes.machine_path(@conn, :index), class: "navbar-item" %>
              <% end %>
            </div>

            <div class="navbar-end">
              <div class="navbar-item">
                <div class="buttons">
                <%= if @namespace do %>
                  Namespace: <strong><%= @namespace %></strong>
                <% end %>
                  <%= if @current_user do %>
                    <%= link "Logout", to: Routes.session_path(@conn, :delete, current_session(@conn)), method: :delete, class: "button is-rounded is-primary" %>
                  <% else %>
                    <a href="<%= Routes.user_path(@conn, :new) %>" class="button is-rounded is-primary">
                      <strong>Sign up</strong>
                    </a>
                    <a href="<%= Routes.session_path(@conn, :new) %>" class="button is-rounded is-light">
                      Log in
                    </a>
                  <% end %>
                </div>
              </div>
            </div>
          </div>
      </div>
    </nav>

    <section id="main" class="section">
      <div class="container">
        <div id="notifications">
          <%= if info = get_flash(@conn, :info) do %>
            <div class="notification is-info" role="alert"> <button class="delete"></button><%= info %></div>
          <% end %>
          <%= if error = get_flash(@conn, :error) do %>
            <div class="notification is-danger" role="alert"> <button class="delete"></button><%= error %></div>
          <% end %>
        </div>
        <%= render @view_module, @view_template, assigns %>
      </div>
    </section>

    <footer class="footer">
      <div class="content has-text-centered">
        <p>
          <strong>Runhyve</strong> made with <i class="has-text-danger fa fa-heart"></i> in Poland. <br /> Powered by <a href="https://runateam.com">Runateam.com</a>.
        </p>
        <p><%= version() %></p>
      </div>
    </footer>

    <script type="text/javascript" src="<%= Routes.static_path(@conn, "/js/app.js") %>"></script>
  </body>
</html>
